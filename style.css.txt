/* style.css */

body {
    font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    background-color: #f4f7f6;
    margin: 0;
    padding: 20px;
    display: flex;
    flex-direction: column;
    align-items: center;
}

h1 {
    color: #333;
    margin-bottom: 20px;
}

/* Container Tabel */
table {
    border-collapse: collapse;
    width: 90%;
    max-width: 1000px;
    background-color: #fff;
    box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
    border-radius: 8px;
    overflow: hidden; /* Agar sudut rounded terlihat */
}

/* Header Tabel */
thead {
    background-color: #2ecc71; /* Warna hijau khas Spreadsheet */
    color: white;
}

th {
    padding: 15px;
    text-align: left;
    text-transform: uppercase;
    font-size: 14px;
    letter-spacing: 0.5px;
}

/* Baris dan Kolom */
td {
    padding: 12px 15px;
    border-bottom: 1px solid #ddd;
    color: #555;
    font-size: 14px;
}

/* Warna baris selang-seling (Zebra Striping) */
tbody tr:nth-child(even) {
    background-color: #f9f9f9;
}

/* Efek Hover */
tbody tr:hover {
    background-color: #f1f1f1;
    transition: background-color 0.3s ease;
}

/* Responsif untuk HP */
@media (max-width: 600px) {
    table {
        font-size: 12px;
    }
    th, td {
        padding: 10px;
    }
}
